#pragma once
void NewFunction(int i, const Button &aButton, int j, const Button &bButton, const Machine &machine, const std::pair<int, int> &prizeLocation, int &cost, int &retFlag);

void getAttemptCost(int i, const Button & aButton, int j, const Button & bButton, const Machine & machine, const std::pair<int, int> & prizeLocation, int &cost, int &retFlag);
